# Definition Snapshot

Real runs copy the governing workflow and stage contracts here at creation time, together with a hash manifest. The snapshot is execution evidence, not a second canonical workflow.
