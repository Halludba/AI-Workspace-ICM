# Workflow Template

This directory is a non-executable scaffold for new workflows.

Do not put domain data, live run artifacts, or production decisions here.
Clone or scaffold it into a new kebab-case workflow directory, then replace generic text with the specialized process.

The template intentionally starts with one generic stage.
Add, rename, or remove stages in the created workflow and keep `WORKFLOW.json` transitions synchronized.
