# Archive

Historical material only.

Archive content is non-authoritative and must not be treated as current workspace state unless the user explicitly asks for historical behavior or evidence.

Do not use archive files as an implicit fallback when current contracts are missing.
