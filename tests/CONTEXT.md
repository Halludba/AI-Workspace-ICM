# Tests Context

Machine-verifiable invariants live here. During iteration, run the narrowest relevant tests. Broaden toward whole-workspace verification only when closure requires it. Passing tests are evidence, not authority; tests must encode current contracts rather than stale assumptions.
