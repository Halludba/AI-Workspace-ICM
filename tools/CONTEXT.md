# Tools Context

Deterministic software lives here. Load only the tool relevant to the selected operation plus its tests/contracts. Tools enforce mechanical invariants; they do not decide semantic policy. Tool output is derived evidence unless a governing contract promotes a result into canonical state.
